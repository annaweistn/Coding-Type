function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(0);
  fill(255);
  textFont("Cormorant Garamond")
  textSize(50);
  textAlign(CENTER, CENTER);
  text("health matters", width / 2, height / 1.5);
  text("health matters", width / 2, height / 2);
  text("health matters", width / 2, height / 3);
}